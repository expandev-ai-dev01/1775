/**
 * @summary
 * Internal (authenticated) API routes for v1
 *
 * @module routes/v1/internalRoutes
 *
 * @description
 * Defines all authenticated API endpoints that require valid user sessions.
 * All routes in this module are protected by authentication middleware.
 */

import { Router } from 'express';

const router = Router();

export default router;
